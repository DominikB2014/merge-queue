name: pull request

on: pull_request

jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - name: Setup node
        uses: actions/setup-node@v2
        with:
          node-version: '14'

      - run: echo "hello"
